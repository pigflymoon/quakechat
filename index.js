/**
 * @format
 */

import {AppRegistry} from 'react-native';

import TabsContainer from './app/Tabs';

import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => TabsContainer);
